<table>
  <tr>
    <th>Name</th>
    <th>Homepage</th>
  </tr>
  <% @authors.each do |author| %>
    <tr>
      <td><%= author.name %></td>
      <td><%= link_to 'Show', author_path(author) %></td>
    </tr>
  <% end %>
  <%= link_to 'New', new_author_path %>
</table>